# Hardening role

Removes `hardening_remove_packages`, applies baseline sysctl settings, and can
disable core dumps through `hardening_disable_core_dumps`. Validate these
settings against the workload and kernel policy before rollout.
