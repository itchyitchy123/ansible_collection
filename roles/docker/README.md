# Docker role

Installs Docker from the approved distribution repository, enables it, and adds
`docker_users` to the Docker group. Review the repository policy first; the
distribution package may not be current Docker CE.
