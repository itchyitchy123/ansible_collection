# Prometheus role

Installs and enables `prometheus_package_name`. Ensure the approved platform or
monitoring repository provides the selected package, then set
`prometheus_repository_configured=true` before execution.
