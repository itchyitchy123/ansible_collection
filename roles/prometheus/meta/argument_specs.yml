---
argument_specs:
  main:
    short_description: Install and enable Prometheus from an approved repository.
    options:
      prometheus_package_name:
        type: str
      prometheus_repository_configured:
        type: bool
