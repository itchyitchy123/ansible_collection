# Grafana role

Installs and enables `grafana_package_name`. Configure an approved Grafana
vendor repository before invoking the role, then set
`grafana_repository_configured=true`; see the compatibility matrix.
